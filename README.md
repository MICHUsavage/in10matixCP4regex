# Laboratory on Regular Expressions

## Tasks

The tasks are located in the `tasks` folder.  
Each task contains a C++ file with the problem description and example implementation.

The exercises focus on using the C++ `<regex>` library.
